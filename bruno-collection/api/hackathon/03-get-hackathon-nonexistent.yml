info:
  name: get-hackathon-nonexistent
  type: http
  seq: 3
http:
  method: GET
  url: '{{base}}/method/fossunited.api.hackathon.get_hackathon?name=DOES-NOT-EXIST-999'
runtime:
  auth:
    type: none
tests:
  value: |
    test("should return error for nonexistent hackathon", function() {
      const data = res.getBody();
      expect(res.getStatus()).to.not.equal(200);
    });
docs: |-
  Error handling for invalid hackathon ID. Should return DoesNotExistError,
  not leak stack traces or internal state.
