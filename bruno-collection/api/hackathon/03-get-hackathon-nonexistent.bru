meta {
  name: get-hackathon-nonexistent
  type: http
  seq: 3
}

get {
  url: {{base}}/method/fossunited.api.hackathon.get_hackathon?name=DOES-NOT-EXIST-999
  body: none
  auth: none
}

docs {
  # Error handling for invalid hackathon ID

  Verifies proper error response for nonexistent hackathon IDs.
  Should return DoesNotExistError, not leak stack traces or internal state.

  **Endpoint**: `fossunited.api.hackathon.get_hackathon`
  **Auth**: `allow_guest=True`
  **Seed data**: none (intentionally uses nonexistent ID)
  **Source**: `fossunited/api/hackathon.py`
}

tests {
  test("should return error for nonexistent hackathon", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.not.equal(200);
  });
}
