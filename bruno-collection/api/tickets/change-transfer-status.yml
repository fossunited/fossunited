info:
  name: change-transfer-status
  type: http
  seq: 4
http:
  method: POST
  url: '{{base}}/method/fossunited.api.tickets.change_transfer_status'
  body:
    type: form-urlencoded
    data:
    - name: transfer_id
      value: '{{transfer_id}}'
    - name: status
      value: '{{status}}'
runtime:
  auth:
    type: apikey
    key: Authorization
    value: '{{auth_token}}'
    placement: header
tests:
  value: |
    test("rejects invalid status values", function() {
      // When status is not Completed or Cancelled, should return error
      const status = bru.getVar("status");
      if (status && status !== "Completed" && status !== "Cancelled") {
        expect(res.getStatus()).to.not.equal(200);
      }
    });

    test("rejects guest access", function() {
      // This endpoint checks for Guest and throws AuthenticationError
      // Verify it requires login even though allow_guest=True on decorator
    });
docs: |-
  SECURITY: Status allowlist enforced (Completed/Cancelled only).
  Guest check via frappe.session.user == "Guest".
  Auth enforced in controller before_save via ignore_permissions.

  allow_guest: true | Rate: 3/12h

  Source: fossunited/api/tickets.py

  Args: transfer_id: str, status: str
