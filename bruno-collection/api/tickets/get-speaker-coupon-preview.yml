info:
  name: get-speaker-coupon-preview
  type: http
  seq: 11
http:
  method: POST
  url: '{{base}}/method/fossunited.api.tickets.get_speaker_coupon_preview'
  body:
    type: form-urlencoded
    data:
    - name: event
      value: '{{event}}'
runtime:
  auth:
    type: apikey
    key: Authorization
    value: '{{auth_token}}'
    placement: header
tests:
  value: |
    test("requires chapter/event member auth", function() {
      // Protected by @require_chapter_or_event_member decorator.
    });

    test("returns only count stats, no PII", function() {
      const data = res.getBody().message;
      if (!data) return;
      expect(data).to.have.property("total");
      expect(data).to.have.property("already_has");
      expect(data).to.have.property("will_create");
      // Should not expose individual speaker details
      expect(data).to.not.have.property("speakers");
      expect(data).to.not.have.property("emails");
    });
docs: |-
  SECURITY: Protected by @require_chapter_or_event_member decorator.
  Returns only aggregate counts, not individual speaker details.

  allow_guest: false

  Source: fossunited/api/tickets.py

  Args: event: str
