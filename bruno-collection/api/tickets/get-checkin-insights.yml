info:
  name: get-checkin-insights
  type: http
  seq: 6
http:
  method: POST
  url: '{{base}}/method/fossunited.api.tickets.get_checkin_insights'
  body:
    type: form-urlencoded
    data:
    - name: event_id
      value: '{{event_id}}'
runtime:
  auth:
    type: apikey
    key: Authorization
    value: '{{auth_token}}'
    placement: header
tests:
  value: |
    test("VULN-008: requires authentication", function() {
      // This endpoint requires auth (no allow_guest)
    });

    test("returns expected structure", function() {
      const data = res.getBody().message;
      if (!data) return;
      expect(data).to.have.property("daily_data");
      expect(data.daily_data).to.be.an("array");
    });
docs: |-
  SECURITY: VULN-008 (MED) - rate limit added (10/1h).
  Requires authentication.

  allow_guest: false | Rate: 10/1h

  Source: fossunited/api/tickets.py

  Args: event_id: str
