info:
  name: get-tickets-insights
  type: http
  seq: 5
http:
  method: POST
  url: '{{base}}/method/fossunited.api.tickets.get_tickets_insights'
  body:
    type: form-urlencoded
    data:
    - name: event_id
      value: '{{event_id}}'
runtime:
  auth:
    type: apikey
    key: Authorization
    value: '{{auth_token}}'
    placement: header
tests:
  value: |
    test("VULN-008: requires authentication", function() {
      // This endpoint requires auth (no allow_guest)
      // Without auth_token, should return 403
    });

    test("returns expected structure", function() {
      const data = res.getBody().message;
      if (!data) return;
      expect(data).to.have.property("total_sold");
      expect(data).to.have.property("tshirt_insights");
      expect(data).to.have.property("tier_data");
    });
docs: |-
  SECURITY: VULN-008 (MED) - rate limit added (10/1h).
  Requires authentication.

  allow_guest: false | Rate: 10/1h

  Source: fossunited/api/tickets.py

  Args: event_id: str
