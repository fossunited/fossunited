info:
  name: get-ticket-tiers
  type: http
  seq: 8
http:
  method: POST
  url: '{{base}}/method/fossunited.api.tickets.get_ticket_tiers'
  body:
    type: form-urlencoded
    data:
    - name: event_id
      value: '{{event_id}}'
runtime:
  auth:
    type: apikey
    key: Authorization
    value: '{{auth_token}}'
    placement: header
tests:
  value: |
    test("VULN-008: requires authentication", function() {
      // This endpoint requires auth (no allow_guest)
    });

    test("returns only title and maximum_tickets per tier", function() {
      const data = res.getBody().message;
      if (!data || !Array.isArray(data) || data.length === 0) return;
      data.forEach(function(tier) {
        expect(tier).to.have.property("title");
        expect(tier).to.have.property("maximum_tickets");
      });
    });
docs: |-
  SECURITY: VULN-008 (MED) - rate limit added (10/1h).
  Returns only title and maximum_tickets (safe fields).

  allow_guest: false | Rate: 10/1h

  Source: fossunited/api/tickets.py

  Args: event_id: str
