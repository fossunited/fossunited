info:
  name: is-ticket-live
  type: http
  seq: 9
http:
  method: POST
  url: '{{base}}/method/fossunited.api.tickets.is_ticket_live'
  body:
    type: form-urlencoded
    data:
    - name: event_id
      value: '{{event_id}}'
runtime:
  auth:
    type: apikey
    key: Authorization
    value: '{{auth_token}}'
    placement: header
tests:
  value: |
    test("returns boolean only", function() {
      const data = res.getBody().message;
      if (data !== null && data !== undefined) {
        expect(typeof data).to.equal("boolean");
      }
    });
docs: |-
  SECURITY: Low risk. Returns only a boolean.
  Guest-accessible for ticket purchase flow.

  allow_guest: true

  Source: fossunited/api/tickets.py

  Args: event_id: str
