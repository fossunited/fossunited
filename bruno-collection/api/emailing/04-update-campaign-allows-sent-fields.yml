info:
  name: update-campaign-allows-sent-fields
  type: http
  seq: 5
http:
  method: POST
  url: '{{base}}/method/fossunited.api.emailing.update_campaign'
  body:
    type: json
    data: |-
      {
        "campaign_id": "{{newsletter_id}}",
        "data": {"email_sent": true, "email_sent_at": "2026-01-01 00:00:00", "total_recipients": 1}
      }
runtime:
  auth:
    type: none
tests:
  value: |
    test("chapter lead can correct the send-status fields", function() {
      const status = res.getStatus();
      expect(status).to.equal(200);
    });
docs: |-
  Positive-path counterpart to 01-update-campaign-rejects-non-allowed.yml.
  email_sent/email_sent_at/total_recipients are now in ALLOWED_FIELDS.
